# 星眠 v1.1.0 验收记录

v1.0.0 的冻结记录与对应源码保存在 `v1.0.0` 标签及独立发布目录中。

## 已完成

- TypeScript 检查和 Vite / Electron 构建通过。
- 36 项核心测试通过：原有调度与存储回归，以及快捷计时的秒级边界/跨午夜/重启/编辑/时区切换、暂停与恢复、真实闹钟暂停、故障保护、导入去重/过期/大小限制、迁移备份与失败保留原数据。
- Electron 40.10.6 上 26 项 Playwright 桌面检查通过：18 项基础流程和 8 项新版流程，覆盖快捷计时、主界面与提醒中的暂停、重启保持暂停、托盘菜单回调、导入预览/确认/默认停用、导出、错误文件、长名称和缩放。
- 两版打包程序共用隔离测试目录，验证 v1.0.0 → v1.1.0 的计划标识与启用状态保留、版本 2 数据写入、原始版本 1 备份不变以及暂停后重新启动。
- 隐藏窗口后到点调用了主进程关机适配器，由安全测试模式拦截，日志确认没有实际关机。
- 窗口尺寸：1240×820、1000×700、900×650 DIP；另用 Electron 设备缩放参数验证 125% 与 150% 布局，未修改 Windows 全局缩放设置。底部操作栏保持可见，长列表在内部滚动。
- 初始安装包验证通过：静默安装至隔离测试目录、桌面快捷方式目标检查、打包版启动和演示、卸载后程序与快捷方式移除。本轮 UI 与作者资料更新后已重新打包，并验证新版打包应用启动与演示；为保留已有用户安装，本轮未重复卸载流程。
- 界面复核的三项发现均已解决：底部栏溢出、筛选/动作类型的选中语义、停用行文字与操作控件的对比度。

## 证据

公开仓库保留 `docs/images/` 中的实际截图与 `docs/verification.json` 中去除本机路径后的结果。
以下原始测试产物仅在开发机保存，不提交用户数据目录或包含机器路径的日志。

- `artifacts/qa-results.json`：桌面自动检查结果和隐藏后资源采样。
- `artifacts/qa-v11-results.json`：新版功能与缩放检查。
- `artifacts/upgrade-results.json`：两版打包程序的数据升级结果。
- `artifacts/installed-smoke.json`：确认新版产物为 packaged=true、版本 1.1.0、Electron 40.10.6。
- `artifacts/install-check.json`：安装、快捷方式、启动及卸载结果。
- `artifacts/01-empty.png` 至 `08-installed.png`：真实 Electron 截图。带计划的截图使用隔离测试数据，正式安装不会预置测试计划。

## 测试边界

- 未实际关闭当前电脑。真实适配器的固定命令为 Windows `shutdown.exe /s /f /t 0`；自动测试替换或拦截其执行。
- 文件流程验证替换了原生对话框的返回值，并实际读取/写入隔离文件；未自动点击 Windows 文件选择器。托盘暂停验证执行了实际菜单回调，未自动点击 Windows 托盘弹出菜单。
- 睡眠、时钟前后调整以模拟时钟/调度重建验证，未让本机真正休眠或更改系统时间。
- 音效合成、播放调用与停止流程已执行；听感及系统静音、外部扬声器情况需要使用者通过「试听闹钟」确认。
- 窗口隐藏后的单次采样显示各 Electron 进程 CPU 使用率为 0%，属于短时测量，不代表所有硬件或场景的性能保证。
- 安装包未进行商业代码签名，签名检查结果为 NotSigned。

软件不创建 Windows 任务计划、开机启动项、常驻服务或唤醒计时器。只有程序正在运行时，关机与闹钟才有效。
