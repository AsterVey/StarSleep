# 星枢 v1.9.1 验证记录

验证日期：2026-09-22。Windows x64，同机运行已打包程序。桌面测试使用独立 `STARSLEEP_QA_DATA` 和 `--safe-mode`，没有执行真实关机。

## 结果

- TypeScript 检查、Vite 构建、Electron NSIS 打包通过。
- 核心自动化：153 项通过，0 失败，包含新增镜头时间轴的阶段、边界、连续性和与采样帧率无关的断言。
- 打包程序桌面检查：6 组通过，页面错误 0。
  - 三阶段开场自动结束，只保留一个窗口和一个场景 Canvas。
  - 观景重播、立即跳过、焦点返回导航。
  - 演示关机提醒打断开场并冻结场景，提醒关闭后正常操作。验证的是共用提醒路径，不是实际关机。
  - 开场中隐藏窗口后，稳定采样期间装饰绘制计数增量为 0；重新显示后可按 Esc 返回。
  - 900×650 窗口、200% 缩放下跳过按钮在可见区域，暂停可用；减少动态效果后停止动态绘制。
  - 关闭自动开场后仍可手动重播；切换静态档位中止开场，重载后直接进入工作台。
- 独立标题断言通过：抵达阶段标题透明度超过 0.95，发生在开场结束之前。
- 分阶段截图完成一轮检查及修正后确认；修正包括边缘光带淡出、静态模式入口和关闭自动播放后的手动重播。

测试脚本：`tests/launch.test.ts`、`scripts/qa-v191.cjs`、`scripts/qa-launch-title-v191.cjs`。证据附于交付目录 `verification`；实际程序录屏由 `scripts/record-launch-v191.cjs` 生成。

## 范围与限制

本轮聚焦开场效果，没有修改调度器、持久化格式、真实客户端配置或关机执行器。没有安装覆盖旧版、卸载、实际关机或远程发布。安装包构建和其解包程序已验证，安装向导没有实际安装验收。

没有重新执行全部旧版工坊与三端配置桌面流程，也没有重新测量 CPU/GPU 功耗或承诺所有设备达到 60 FPS。沿用现有自适应降档与 WebGL 静态回退。资源释放由退出路径实现，未做长时间 GPU 显存泄漏压力测试。
