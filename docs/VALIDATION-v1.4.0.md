# v1.4.0 开发预览验证记录

日期：2026-09-22。所有桌面测试使用独立数据目录和安全关机替代器。

| 检查 | 结果 |
| --- | --- |
| 核心测试 | 87 项通过：包含已有调度、存储、体验功能及新增联动、导入、配置与还原 |
| v1.3 桌面回归 | 14 项通过：模板、七天、迷你模式、提醒、托盘、重启及显示缩放 |
| Agent 与主题桌面流程 | 8 组通过：导入、hooks 合并、PowerShell helper、任务选择、隐藏观察与提醒、安全替代执行、暂停重启、缩放 |
| 原客户端配置面板 | 最终打包 EXE 中 4 组通过：Codex 配置/选择性还原、Claude 配置/还原、900×650 DIP 和 100/125/150/200% 显示缩放、波纹帧时间回归 |
| 当前 Codex 配置副本 | 应用后还原，与原副本逐字一致；真实配置未修改 |
| 生产依赖审计 | 0 项已知漏洞（本次 npm audit --omit=dev 结果） |
| 构建 | TypeScript 与生产构建通过 |
| 最终打包 EXE | 配置面板 4 组与 Agent/美化桌面流程 8 组全部通过，渲染错误为空；只运行 win-unpacked 中的 EXE，没有安装到用户当前目录 |

## 证明范围

PowerShell helper 使用隔离模拟 Agent 进程，实际执行了父进程识别和本机 HTTP 通信。桌面联动测试等待真实 60 秒观察期与 1 分钟提醒，最终执行安全替代器。它证明了星眠侧闭环，**不证明每个真实 Agent 客户端都已经完成端到端接入**。

Codex 原生主题字段与持久化格式通过只读检查本机客户端外观实现核实。配置测试使用实际配置副本；未更换当前正在工作的 Codex 主题。Claude 写入/恢复仅在隔离目录验证。本机商店版不兼容本次加载器适配，显示为不能直接应用。

截图是星眠配置助手界面的测试截图，不能用作 Codex 或 Claude 已完成换肤的证据。

## 未执行

- 真实 Windows 强制关机。
- Codex 原界面更换主题后的视觉核验。
- 当前 Claude 商店版的加载器安装或修改程序。
- 三种客户端的真实模型任务完成到关机闭环。
- 当前用户安装上的升级、卸载与系统睡眠实机测试。

开发复现：`npm test`、`npm run build`、`node scripts/qa-v13.cjs`、`node scripts/qa-v14.cjs`、`node scripts/qa-client-themes.cjs`。最后三项会打开隔离的安全体验窗口，不会关闭电脑。
