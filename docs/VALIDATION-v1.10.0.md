# 星枢 v1.10.0 验证记录

验证日期：2026-09-23。Windows x64；所有自动化使用隔离目录和 `--safe-mode`。未实际关机、安装覆盖或修改真实 Agent 配置。

## 构建与核心

- TypeScript 类型检查、Vite 构建和 Electron NSIS 安装包构建通过。
- 核心测试 160 项全部通过，0 失败、0 跳过。
- 新增 7 项测试覆盖：并发扫描共用结果、强制刷新与缓存时效、失败重试、参数拒绝、系统时钟回拨、每日汇总与来源筛选、空用量及命令搜索与排序（部分行为在同一测试中）。
- 未增加依赖；继续使用安装包自带的 ccusage 20.0.24 MIT 引擎。

## 已打包程序的桌面验证

新工作台 6 组检查通过：

1. Ctrl+K / 点击入口、关键词、无结果反馈、方向键、Enter 与焦点恢复。
2. 真实 ccusage 引擎读取隔离 JSONL 测试日志；手动刷新由 11,000 更新到 22,000 Token，缓存读取占输入为 40.0%；缺失来源明确标注，非法刷新参数被主进程拒绝。
3. 切换工具页保留来源、时间范围、搜索及未保存备注；用量页不在前台时，模拟推进 10 分钟没有新增用量 IPC 请求。
4. 运行摘要、待处理项筛选；注入体检失败，再恢复接口并重试，旧错误清除。
5. 快捷入口只打开编辑器、不创建计划；编辑时不能覆盖面板；演示关机提醒优先显示并关闭快捷面板。
6. 900×650 起窗口及 100% / 125% / 150% / 200% 缩放，导航、快捷选择、导出入口可达，文档无横向溢出。

旧版工具箱回归 6 组检查通过：

1. 两种日志格式统计、日期与来源筛选、脱敏用量导出。
2. 开源资源搜索、分类、收藏、备注、复制及限定来源链接。
3. 本机 Skills 清单、元数据搜索、仅允许打开扫描出的文件位置。
4. 收藏及保存备注重启恢复，非法日志目录不改变配置。
5. 桌面、小窗口及 125% / 150% / 200% 缩放的工具箱操作。
6. 提醒覆盖工具箱，处理后可以返回计划。

两组脚本页面错误均为 0。界面完成一次批量检查与集中修正，再对打包版确认。用量截图中的数字来自测试夹具，不是用户真实消耗。

## 可复核材料与边界

交付 `verification/core-tests.txt`、`verification/workspace-results.json`、`verification/toolbox-results.json` 以及截图。对应脚本为 `scripts/qa-v110.cjs` 与 `scripts/qa-toolbox.cjs`。

主进程将两个用量来源改为并行读取，并隔离引擎缓存目录；未进行大型真实日志上的耗时或功耗基准，不宣称固定加速倍数。页面保留已访问工具组件以保存草稿，关闭窗口/程序时的行为遵循现有规则；备注仍需用户点击保存才持久化。

本轮未重新做安装向导、卸载、三端真实主题效果、真实夜间联动关机和 GPU 长时间压力测试。安装包构建及其解包程序已验证。计划存储、调度器和接入协议没有更改，未引入迁移；没有新增 DeepSeek / Claude Chat Token 数据源。
